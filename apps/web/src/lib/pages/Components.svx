<script lang="ts">
  import FrameworkTabs from '$lib/components/site/framework-tabs.svelte';
  import ComponentDocs from '$lib/components/docs/ComponentDocs.svelte';
  import { getPage } from '$lib/page.svelte.js';
  const page = getPage();
  const framework = $derived(page.url.pathname.startsWith('/react') ? 'React' : page.url.pathname.startsWith('/vue') ? 'Vue' : 'Svelte');
  let { data } = $props();
</script>

# Components

<FrameworkTabs />

Weather, moon, and tide components for {framework} that inherit your {framework === 'React' ? 'shadcn/ui' : framework === 'Vue' ? 'shadcn-vue' : 'shadcn-svelte'} theme, base components, and icon library.

## Time zones

All cards use an explicit IANA `timeZone` prop first, then `location.timeZone`, then the visitor’s browser time zone after hydration (UTC during server rendering). `ForecastDashboard` forwards its explicit `timeZone` prop to all three cards. Location coordinates and the provider’s time zone remain separate from the display time zone.

<ComponentDocs {data} />
