# React preview

Run `pnpm install` and `pnpm dev:react` from the workspace root. `pnpm build:react` typechecks and builds the native Vite app. The same React preview is served by Astro at `/react`. Controls cover theme, units, size, density, detail, and atmospheric motion. All displayed data is labeled as fixtures.
